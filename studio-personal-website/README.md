# Personal Website Studio

Sanity Studio for the personal website content.

This project targets Sanity project `pwlv2v22` and dataset `production`.

The current Sanity CLI requires Node `>=20.19 <22` or `>=22.12`. After switching Node versions and logging in, run:

```sh
npm install
npm run dev
```
